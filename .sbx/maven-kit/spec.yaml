schemaVersion: "2"
kind: mixin
name: maven
version: "1"
description: Install Maven for Java builds.

setup:
  install:
    - command: apt-get update && apt-get install -y --no-install-recommends maven
