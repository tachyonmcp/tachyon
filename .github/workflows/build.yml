name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  merge_group:
    types: [ checks_requested ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  checks: write
  pull-requests: write
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        java:
          - version: 21
            distribution: temurin
            command: "clean lint build revapi jmh"
            codecov: true
          - version: 25
            distribution: temurin
            command: "clean build jmh"
            codecov: false
          - version: 27
            distribution: zulu
            command: "clean build jmh"
            codecov: false
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK ${{ matrix.java.version }}
        uses: actions/setup-java@v6
        with:
          java-version: ${{ matrix.java.version }}
          distribution: ${{ matrix.java.distribution }}
          cache: maven

      - name: Build and Verify
        run: make ${{ matrix.java.command }}

      - name: Publish test results
        uses: mikepenz/action-junit-report@v6
        if: ${{ !cancelled() && github.event.pull_request.head.repo.fork != true }}
        with:
          report_paths: '**/target/*-reports/TEST-*.xml'
          check_name: Test Report (JDK ${{ matrix.java.version }})
          detailed_summary: true
          flaky_summary: true
          include_passed: false
          fail_on_failure: true
          require_tests: true
          comment: true
          job_summary: true

      - name: Publish coverage report
        uses: madrapps/jacoco-report@v1.8.0
        if: ${{ !cancelled() && github.event.pull_request.head.repo.fork != true }}
        with:
          paths: 'reports/target/site/jacoco-aggregate/jacoco.xml'
          title: Coverage Report (JDK ${{ matrix.java.version }})
          update-comment: ${{ github.event_name == 'pull_request' }}
          min-coverage-changed-files: 75
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload results to Codecov
        if: ${{ !cancelled() && matrix.codecov && github.event.pull_request.head.repo.fork != true }}
        uses: codecov/codecov-action@v7
        with:
          name: codecov-jdk${{ matrix.java.version }}
          files: 'reports/target/site/jacoco-aggregate/jacoco.xml'
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: tachyonmcp/tachyon

      - name: Upload test results to Codecov
        if: ${{ !cancelled() && matrix.codecov && github.event.pull_request.head.repo.fork != true }}
        uses: codecov/codecov-action@v7
        with:
          report_type: test_results
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload reports as artifacts
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: reports-jdk-${{ matrix.java.version }}
          path: |
            **/target/*-reports/**
            reports/target/site/jacoco-aggregate/
            **/spotbugsXml.xml
            **/target/revapi-report.txt

  examples-snapshot:
    strategy:
      fail-fast: false
      matrix:
        java:
          - { version: 21, distribution: temurin }
        example:
          - echo-kotlin
          - langchain4j-mcp
          - mcp-java
          - temporal
          - weather-mcp
          - weather-mcp-kotlin
          - weather-mcp-spring-boot

    runs-on: ubuntu-latest
    timeout-minutes: 7

    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK ${{ matrix.java.version }}
        uses: actions/setup-java@v6
        with:
          java-version: ${{ matrix.java.version }}
          distribution: ${{ matrix.java.distribution }}
          cache: maven

      - name: Install jar
        run: |
          make package

      - name: Verify examples
        working-directory: 'examples/${{ matrix.example }}'
        run: |
          ./mvnw verify -Dtachyon.version=1.0.0-SNAPSHOT --no-transfer-progress
          rm -rf ~/.m2/repository/dev/tachyonmcp

      - name: Publish test results
        uses: mikepenz/action-junit-report@v6
        if: ${{ !cancelled() && github.event.pull_request.head.repo.fork != true }}
        with:
          report_paths: 'examples/${{ matrix.example }}/target/*-reports/TEST-*.xml'
          check_name: 'Test Report: ${{ matrix.example }} (JDK ${{ matrix.java.version }})'
          detailed_summary: true
          flaky_summary: true
          include_passed: false
          fail_on_failure: true
          require_tests: true
          comment: true
          job_summary: true

      - name: Upload reports as artifacts
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: reports-jdk-${{ matrix.java.version }}
          path: '**/target/*-reports/**'

