# Copyright (c) 2026 Konstantin Pavlov/IT Staff and contributors.
#
# Shared by every language in .github/workflows/codeql.yml.

name: Tachyon MCP

paths-ignore:
  # Fixtures, not shipped code: skill scripts and sample payloads exist to be loaded, not run.
  - '**/src/test/resources/**'
  # Generated from tachyon-core/protocol/*.ts at build time; fix the generator, not the output.
  - '**/target/generated-sources/**'
